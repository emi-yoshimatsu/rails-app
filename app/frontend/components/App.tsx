import React from "react"
import { Button } from "my-ui-library" // ライブラリから読み込む

export default function App() {
  return (
    <div>
      <h1>Hello from Vite + Rails + React</h1>
      <Button />
    </div>
  )
}